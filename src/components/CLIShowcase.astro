<div class="cli-showcase">
  <article class="terminal-card" aria-labelledby="server-cli-title">
    <header>
      <div aria-hidden="true"><i></i><i></i><i></i></div>
      <span id="server-cli-title">Machine with the service</span>
    </header>
    <pre tabindex="0" aria-label="WarpTweet host command example"><code>{`$ warptweet host --to 127.0.0.1:5432 --name staging-db --access-for 30d

host ready
target  127.0.0.1:5432
invite  staging-db.wtinvite
class   confidential bearer; transfer authenticated and delete after use
access  30d`}</code></pre>
  </article>

  <article class="terminal-card terminal-card--client" aria-labelledby="client-cli-title">
    <header>
      <div aria-hidden="true"><i></i><i></i><i></i></div>
      <span id="client-cli-title">Your laptop</span>
    </header>
    <pre tabindex="0" aria-label="WarpTweet connect command example"><code>{`$ warptweet connect staging-db.wtinvite --listen-port 15432 --restart unless-stopped

connected
open     127.0.0.1:15432
service  127.0.0.1:5432

# point your app at 127.0.0.1:15432`}</code></pre>
  </article>
</div>
